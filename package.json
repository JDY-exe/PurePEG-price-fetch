{
  "name": "monorepo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npx playwright install --with-deps && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}